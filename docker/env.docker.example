# =============================================================================
# MCP-EDA Docker Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment
# This file contains all environment variables needed for Docker deployment

# =============================================================================
# OpenAI API Configuration (REQUIRED)
# =============================================================================
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_MODEL=gpt-4

# =============================================================================
# EDA Tools Configuration
# =============================================================================
# Path to EDA tools installation (adjust for your system)
EDA_TOOLS_HOST=host.docker.internal
SYNOPSYS_ROOT=/opt/synopsys
CADENCE_ROOT=/opt/cadence

# License server configuration
SNPSLMD_LICENSE_FILE=27000@license-server.company.com
CDS_LIC_FILE=5280@license-server.company.com

# =============================================================================
# Service Ports Configuration
# =============================================================================
# These ports should match the ones defined in docker-compose.yml
SYNTH_SERVER_PORT=13333
UNIFIED_PLACEMENT_SERVER_PORT=13340
CTS_SERVER_PORT=13338
UNIFIED_ROUTE_SAVE_SERVER_PORT=13341

# Agent and MCP server ports
MCP_AGENT_PORT=8000
MCP_SERVER_PORT=8001

# =============================================================================
# Logging and Debug Configuration
# =============================================================================
LOG_ROOT=./logs
LOG_LEVEL=info
DEBUG=false

# =============================================================================
# Docker Compose Configuration
# =============================================================================
# Set to 'true' to enable optional services
CHECK_ALL_SERVICES=false

# Compose profiles to enable
# Available profiles: experiment, test
COMPOSE_PROFILES=

# =============================================================================
# Development Configuration (Optional)
# =============================================================================
# Uncomment and modify as needed for development

# Python configuration
# PYTHONPATH=/app
# PYTHONUNBUFFERED=1

# Additional debug settings
# FASTAPI_DEBUG=false
# UVICORN_LOG_LEVEL=info

# =============================================================================
# Security Configuration (Optional)
# =============================================================================
# Uncomment for production deployments

# API security
# API_KEY_HEADER=X-API-Key
# ALLOWED_HOSTS=localhost,127.0.0.1

# SSL/TLS configuration
# SSL_CERTFILE=/path/to/cert.pem
# SSL_KEYFILE=/path/to/key.pem

# =============================================================================
# Resource Limits (Optional)
# =============================================================================
# Uncomment to set resource limits

# Memory limits (in MB)
# AGENT_MEMORY_LIMIT=1024
# SERVERS_MEMORY_LIMIT=2048
# EXPERIMENT_MEMORY_LIMIT=4096

# CPU limits (as decimal, e.g., 0.5 = 50% of one CPU)
# AGENT_CPU_LIMIT=0.5
# SERVERS_CPU_LIMIT=1.0
# EXPERIMENT_CPU_LIMIT=2.0